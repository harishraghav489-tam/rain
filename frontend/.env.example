# Mapbox GL Access Token (Optional: application defaults to interactive SVG/Canvas if unset)
NEXT_PUBLIC_MAPBOX_TOKEN=

# Backend FastAPI URL (leave empty to use typed local mock services)
NEXT_PUBLIC_API_BASE_URL=

# Environment mode
NEXT_PUBLIC_APP_ENV=development
